<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header %>
    <!-- Add your EJS code here -->
  <ul>
    <% for (let shortURL in urls) { %>
     <li><%= shortURL %></a> : <%= urls[shortURL] %>
       <a href="urls/<%= shortURL %>">Edit</a>
       <form method="POST" action= "/urls/<%= shortURL %>/delete/" >
         <button>Delete 🗑</button>
       </form>
     </li>
    <% } %>
  </ul>
</body>
</html>