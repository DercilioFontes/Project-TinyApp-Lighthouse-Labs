<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
    <ul>
      <% Object.keys(urls).forEach(function (key) { %>
       <li><%= key %> : <%= urls[key] %></li>
      <% }) %>
    </ul>
</body>
</html>